
<div class="container">
  <h1> To do: </h1>
  <ul>
    <% @tasks.each do |task| %>
    <li><%=link_to task.title, task_path(task),
      class: "tasks" %></li>
    <% end %>
  </ul>

  <div class="buttons">
    <%= link_to "Create a new task", "tasks/new",
      class: "btn"%>
  </div>
</div>
